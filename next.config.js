/** @type {import('next').NextConfig} */
const nextConfig = {
  // Habilitar si usas dominios de imágenes de Supabase Storage
  // images: { remotePatterns: [{ hostname: '*.supabase.co' }] },
};

module.exports = nextConfig;
